#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int r, c;
        cin r >> c;
        vector<string> G(r);
        for(int i = 0; i < r; i++)
            cin >> G[i];
    }
    return 0;
}
